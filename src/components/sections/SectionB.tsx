export default function SectionB() {
    return (
        <section id="section-b" className="bg-neutral-950 min-h-screen w-full flex items-center justify-center">
            {/* Section B exists but content is purposely empty as per design request. 
                 min-h-screen ensures it occupies space on the page. */}
        </section>
    );
}
